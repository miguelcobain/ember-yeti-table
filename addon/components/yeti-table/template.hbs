{{yield (hash
  header=(component "yeti-table/header"
    columns=columns onColumnClick=(action "onColumnSort")
    sortable=sortable sortSequence=sortSequence parent=this
  )
  head=(component "yeti-table/head"
    columns=columns onColumnClick=(action "onColumnSort")
    sortable=sortable sortSequence=sortSequence parent=this
  )
  body=(component "yeti-table/body" data=processedData columns=columns parent=this)
  foot=(component "yeti-table/foot" columns=columns parent=this)
  pagination=(component "yeti-table/pagination"
    disabled=isLoading
    paginationData=paginationData
    paginationActions=(hash
      previousPage=(action "previousPage")
      nextPage=(action "nextPage")
      goToPage=(action "goToPage")
      changePageSize=(action "changePageSize")
    )
  )
  actions=(hash
    previousPage=(action "previousPage")
    nextPage=(action "nextPage")
    goToPage=(action "goToPage")
    changePageSize=(action "changePageSize")
  )
  paginationData=paginationData
  isLoading=isLoading
  totalColumns=columns.length
  visibleColumns=visibleColumns.length
  totalRows=normalizedTotalRows
  visibleRows=processedData.length
)}}