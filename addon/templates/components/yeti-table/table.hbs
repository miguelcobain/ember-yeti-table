{{yield (hash
  header=(component "yeti-table/table/header"
    columns=columns onColumnClick=(action onColumnClick)
    sortProperty=sortProperty sortDirection=sortDirection parent=parent
  )
  body=(component "yeti-table/table/body" data=data columns=columns)
)}}