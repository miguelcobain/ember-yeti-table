{{#if visible}}
  <th class="{{class}} {{if isAscSorted "yeti-table-sorted-asc"}} {{if isDescSorted "yeti-table-sorted-desc"}}" onclick={{action onClick}}>
    {{yield (hash
      isSorted=isSorted
      isAscSorted=isAscSorted
      isDescSorted=isDescSorted
    )}}
  </th>
{{/if}}