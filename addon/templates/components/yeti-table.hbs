{{yield (hash
  table=(component "yeti-table/table"
    data=processedData columns=columns onColumnSort=(action onColumnSort)
    sortProperty=sortProperty sortDirection=sortDirection
    parent=this
  )
)}}